import { SettingsView } from "@/components/settings/settings-view";

export const metadata = {
  title: "Configurações | OmniTask",
  description: "Gerencie seu perfil e segurança na OmniTask.",
};

export default function SettingsPage() {
  return <SettingsView />;
}
